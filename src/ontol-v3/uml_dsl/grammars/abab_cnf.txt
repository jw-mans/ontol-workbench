# Грамматика для языка L = {a^n b^n | n >= 1}
# Уже в нормальной форме Хомского (CNF / WCNF)
# Используется с графом paper_double_cycle.csv
# Стартовый нетерминал: S
#
# Исходные правила:   S -> a S b | a b
# После приведения к CNF:
S -> A B
S -> A S1
S1 -> S B
A -> a
B -> b
