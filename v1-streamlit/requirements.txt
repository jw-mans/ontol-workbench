# V1 — веб-ЛК (Streamlit). Рендерит in-process через пакет ontol.
# Само ядро ontol ставится editable из корня репозитория: `pip install -e .`
# (поэтому здесь его нет — иначе подтянулась бы версия с PyPI без наших правок).
#
# V1 — Streamlit web workspace. Renders in-process via the ontol package, which
# is installed editable from the repo root (`pip install -e .`) — intentionally
# NOT listed here, to avoid pulling the PyPI build instead of our local core.
streamlit>=1.43
# Ace-based editor: syntax highlighting + line numbers for the .ontol files.
# Optional — the app falls back to a plain text area if it is missing.
# Редактор на Ace: подсветка синтаксиса и номера строк для .ontol-файлов.
# Опционально — при отсутствии приложение откатывается на обычное текстовое поле.
streamlit-ace>=0.1.1
