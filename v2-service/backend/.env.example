# Скопировать в .env и поправить под себя.
DATABASE_URL=postgresql+asyncpg://ontol:ontol@localhost:5432/ontol
PLANTUML_URL=http://localhost:8080/png/
# ≥32 байт; в проде сгенерировать случайный (напр. `openssl rand -hex 32`).
SECRET=dev-secret-change-me-in-production-0000
