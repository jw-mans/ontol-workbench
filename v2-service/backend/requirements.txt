# V2 backend (FastAPI). Доменное ядро ontol ставится отдельно из корня репо:
#   pip install -e .
# V2 backend deps. The ontol core is installed separately from the repo root:
#   pip install -e .
fastapi>=0.115
uvicorn[standard]>=0.30
sqlalchemy[asyncio]>=2.0
asyncpg>=0.29
alembic>=1.13
pydantic-settings>=2.4
fastapi-users[sqlalchemy]>=13
httpx>=0.27
